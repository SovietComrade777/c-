#include<stdio.h>
int main(){
    int q,x;
    
    scanf("%d%d",&q,&x);
    // scanf("%d",x);
    if(q%x==0){
        printf("%d is a multiple of %d",q,x);
    }
    else{
        printf("%d is a multiple of %d",q,x);
    }
    return 0;
}
