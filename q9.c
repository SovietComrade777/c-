/*Write a C program that accepts two integers from the user and calculates the sum of the two integers.*/#include<stdio.h>
int main(){
    int a,b;
    printf("Input the first Integer:\n");
    scanf("%d",&a);
    printf("Input the second Integer:\n");
    scanf("%d",&b);
    printf("Sum of the above two integers =%d",a+b);
    return 0;
}
